meta {
  name: Delete Workspace
  type: http
  seq: 9
}

delete {
  url: {{BASE_URL}}/api/workspaces/:workspaceId
  body: none
  auth: inherit
}

params:path {
  workspaceId: {{WORKSPACE_ID}}
}

settings {
  encodeUrl: true
  timeout: 0
}
