meta {
  name: Update Workspace
  type: http
  seq: 3
}

patch {
  url: {{BASE_URL}}/api/workspaces/:workspaceId
  body: json
  auth: inherit
}

params:path {
  workspaceId: {{WORKSPACE_ID}}
}

body:json {
  {
    "name": "New Workspace"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
