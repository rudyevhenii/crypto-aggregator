services:
  db:
    image: postgres:16-alpine
    container_name: crypto-db
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: crypto_aggregator_db
    volumes:
      - postgres_data:/var/lib/postgres/data

volumes:
  postgres_data: